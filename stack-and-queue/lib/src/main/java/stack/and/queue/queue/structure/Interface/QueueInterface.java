package stack.and.queue.queue.structure.Interface;

import stack.and.queue.queue.data.QueueNode;

public interface QueueInterface {
    public void enqueue(QueueNode queue);
    public QueueNode dequeue();
}
